A rabbit that can do a couple of things and also set a pomodoro timer for people who are bad at studying.

Run ```dotnet publish -c Release -f net10.0-windows -r win-x64 --self-contained true -p:PublishSingleFile=true -p:Platform="Any CPU"``` to make an exe for yourself.
